# Personal config — never commit these
claude-job-profile.json
CLAUDE.md

# Private context modules — fill these in from the .template.md versions
context/profile.md
context/preferences.md
context/personal-questions.md

# Persona-specific skills — generated by /setup Phase 2.4 from your profile and preferences
# Template versions (no personal content) live in skills/*.template.md
skills/archetypes.md
skills/jd-filter.md
skills/score-rubric.md
skills/star-library.md

# Target company and geography config — user-specific, built up over time
# Template version (structure only) lives in portals.template.yml
portals.yml

# Personal data exports
LinkedIn Data Export*/
*.xlsx
__pycache__/

# Resumes and documents with personal info
*.pdf
*.docx
*.doc
resumes/

# macOS
.DS_Store

# Scan history (dedup state — regenerated on each run)
scanned_history.json

# Per-run Notion scripts — contain hardcoded page IDs and personal data
scripts/notion_log_*.py
scripts/notion_update_*.py

# Working files — intermediate outputs, never commit
working/

# Logs
*.log

# Temp files
*.tmp
*.temp
