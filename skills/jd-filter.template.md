# Skill: JD Filter

**Always active.** Apply whenever evaluating whether a role qualifies for the candidate's pipeline. The title alone is NEVER sufficient — always verify JD content before including or excluding.

---

> **SETUP NOTE:** The include/exclude signals below are generated by `/setup` Phase 2.4
> from `context/preferences.md` (your target role types, tool stack, and deal-breakers).
>
> - Instance file: `skills/jd-filter.md` — gitignored, lives only on your machine
> - This template: `skills/jd-filter.template.md` — in git, no personal content
>
> To regenerate: run `/setup` and go to Phase 2.4.

---

## Include if the JD mentions ANY of:

<!-- INCLUDE SIGNALS GENERATED BY /setup PHASE 2.4 FROM context/preferences.md -->

## Exclude if the JD is primarily about:

<!-- EXCLUDE SIGNALS GENERATED BY /setup PHASE 2.4 FROM context/preferences.md -->

## Ambiguous titles — always fetch full JD before deciding:

<!-- AMBIGUOUS TITLE LIST GENERATED BY /setup PHASE 2.4 FROM context/preferences.md -->

## Language flags — log but don't hard-exclude:

- "Fluent [language] required" → flag as risk in Red Flags, still log if otherwise strong (score 70+)

## Output when applying this skill:

```
JD Filter Result:
  Decision: INCLUDE | EXCLUDE | AMBIGUOUS
  Signals found: [list of matching include signals]
  Exclude signals found: [list if any]
  Red flags: [language requirements, etc.]
  Confidence: HIGH | MEDIUM | LOW
```
